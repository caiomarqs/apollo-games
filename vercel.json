{
    "version": 2,
    "builds": [
        {
            "src": "./server/build",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "./server/(.*)",
            "dest": "/"
        }
    ]
}